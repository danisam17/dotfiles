function changed() {
  if ([ -d .git ] || git rev-parse --git-dir > /dev/null 2>&1); then
    if ! git diff-index --quiet HEAD --; then
      printf '[∴]'
    else
      printf ''
    fi
  fi
}

function prompt() {
  export PS1='  $(whoami) at $(basename `pwd`) $(changed) \n'
}

prompt
